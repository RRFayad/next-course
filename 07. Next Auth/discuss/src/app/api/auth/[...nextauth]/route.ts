export { GET, POST } from "@/auth";

/*
// For an API we wold have to do this manually - Here we don't need to define it ourselves (as they are defined from NextAuth - So we just import and export)
export async function GET() {}
export async function POST() {}
*/
