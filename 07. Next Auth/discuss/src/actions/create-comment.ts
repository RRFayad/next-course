"use server";

export async function createComment() {
  // revalidatePath('/')ans  revalidatePath('/topics/SLUG')are not too relevant, they can be revalidated by time
  //TODO: revalidatePath('/topics/:slug/posts/:postId')
}
